---
import { cn } from "@/lib/utils"

type Props = {
  class?: string
}

const { class: className } = Astro.props as Props
---

<div class={cn("bg-card text-card-foreground rounded-lg border shadow-sm min-h-44", className)}>
  <slot />
</div>
