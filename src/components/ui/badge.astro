---
import { cn } from "@/lib/utils"

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<span
  class={cn(
    "inline-flex items-center rounded-full bg-muted px-3.5 py-1 text-sm font-medium text-muted-foreground ring-offset-background transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2",
    className,
  )}
>
  <slot />
</span>
