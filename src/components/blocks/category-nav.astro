---
import { CATEGORIES } from "@/lib/categories"
import Badge from "@/components/ui/badge.astro"
---

<nav class="mt-3">
  <ul class="flex flex-wrap gap-2">
    {
      CATEGORIES.map((category) => (
        <li>
          <a href={category.url}>
            <Badge class="hover:bg-muted-foreground hover:text-background">
              {category.name}
            </Badge>
          </a>
        </li>
      ))
    }
  </ul>
</nav>
